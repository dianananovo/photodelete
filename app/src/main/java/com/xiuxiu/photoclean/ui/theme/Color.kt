package com.xiuxiu.photoclean.ui.theme

import androidx.compose.ui.graphics.Color

// 马卡龙色系规范 (Macaron Pastel Palette)
val MacaronBlue = Color(0xFF7FAFF6)         // 马卡龙蓝 (纯色，按键与主色)
val MacaronBlueLight = Color(0xFFEEF5FF)    // 极淡马卡龙蓝衬底
val MacaronBlueBorder = Color(0xFFCFE2FE)   // 马卡龙蓝描边

val MacaronPink = Color(0xFFF7A1B5)         // 马卡龙粉 (纯色，删除与高亮)
val MacaronPinkLight = Color(0xFFFFF0F4)    // 极淡马卡龙粉衬底
val MacaronPinkBorder = Color(0xFFFCD5DF)   // 马卡龙粉描边

val MacaronPurple = Color(0xFF9D8DF1)       // 马卡龙紫 (移动相册)
val MacaronPurpleLight = Color(0xFFF4F1FE)

val BackgroundCanvas = Color(0xFFFFFFFF)    // 纯白背景底色 (无杂色)
val CardBackground = Color(0xFFFFFFFF)

val TextPrimary = Color(0xFF1E2229)         // 纯黑质感文字
val TextSecondary = Color(0xFF6B7485)       // 优雅次级灰
val TextMuted = Color(0xFFA0A8B8)

// 兼容别名
val PastelBlue = MacaronBlue
val PastelBlueLight = MacaronBlueLight
val PastelBlueBorder = MacaronBlueBorder
val PastelPink = MacaronPink
val PastelPinkLight = MacaronPinkLight
val PastelPinkBorder = MacaronPinkBorder
val BackgroundApp = BackgroundCanvas
val BackgroundCream = BackgroundCanvas
val BabyBlue = MacaronBlue
val BabyBlueLight = MacaronBlueLight
val BabyBlueGradientStart = MacaronBlue
val BabyBlueGradientEnd = MacaronBlue
val CardSurfaceWhite = Color(0xFFFFFFFF)
val SurfaceSubtle = Color(0xFFF7F9FC)
